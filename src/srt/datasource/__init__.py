"""Define a data access layer that provide uniform access to different datasource

There is no general way to access different kinds of data directly.
So we defined data types and interfaces to access them in a uniform way.

"""

import logging

logger = logging.getLogger(__name__)
